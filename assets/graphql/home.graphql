#import "../assets/graphql/fragments/header.graphql"

query Home {
  home {
    hero {
      headline
      intro
      cta {
        buttons {
          label
          internal
          external
          style
        }
      }
    }
    intro {
      ...HeaderFragment
    }
  }
}
